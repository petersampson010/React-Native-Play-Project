import React from 'react';
import { View, Text } from 'react-native';

export default function NewTeamSetupScreen() {
    return (
      <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
        <Text>New Team Setup Screen</Text>
      </View>
    );
  }