import { vh, vw } from "react-native-expo-viewport-units";

export const profile = {
    width: vw(15),
    height: vh(11),
    resizeMode: 'cover',
    borderRadius: 30,
}

export const profileContainer = {
    flex: 1,
    alignItems: 'center'
}